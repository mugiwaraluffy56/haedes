# syntax=docker/dockerfile:1.7

ARG BASE_IMAGE=haedes-sandbox-base:dev
FROM ${BASE_IMAGE}

LABEL org.opencontainers.image.title="haedes sandbox development image" \
      org.opencontainers.image.description="Default development image for local tests and AWS sandbox tasks"
